import React, { Component } from "react";
import { useParams } from "react-router-dom";
export default class CovidDetail extends Component {
  render() {
    // const watevs = useParams();
    return (
      <React.Fragment>
        <div>Now showing </div>
      </React.Fragment>
    );
  }
}
